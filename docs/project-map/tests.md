---
module: tests
purpose: Backend + frontend unit tests + backend integration tests (PR#44)
key_files:
  - tests/conftest.py — fixtures: mock_settings, mock_db, create_channel/create_job factories, mock_message, mock_telethon_client, mock_telethon_get_message, mock_telethon_edit_message, mock_telethon_get_last_messages (PR#34), mock_runner, authed_client; mock_channel_messages helper + MockMessage.date поле (PR#32) + MockMessage.entities опциональное поле (PR#44); PR#62: MAX_PROBE_ID удалён из mock_settings delenv list
  - tests/test_health.py — TestClient → /health 200
  - tests/test_config.py — Settings: defaults, custom env, frozen check (PR#62: MAX_PROBE_ID удалён из env list + assertion settings.max_probe_id)
  - tests/test_db.py — DB connection layer (6 тестов: file/idempotent/pragmas/schema/get_db/noop-close)
  - tests/test_models_channel.py — channels CRUD (7 тестов: CRUD + UNIQUE + whitelist)
  - tests/test_models_job.py — edit_jobs CRUD (PR#84: +count_jobs 4 теста — all/filter-by-channel/filter-by-status/empty; 5→9 тестов: CRUD + filters + CASCADE + count)
  - tests/test_models_log.py — edit_logs CRUD (4 тестов: CRUD + pagination + CASCADE)
  - tests/test_telegram_client.py — bot-mode Telethon client wrapper (PR#62: 20 тестов — удалены 5 _find_max_id тестов, добавлены 3 get_last_messages с max_id + 4 parse_post_link теста; PR#44: +test_edit_message_entities_invokes_raw_api для messages.EditMessageRequest + InputPeerChannel; PR#64: test_edit_message_success updated — edit_message вызывается с formatting_entities=None kwarg; PR#86: test_edit_message_success/test_edit_message_entities_uses_high_level_api updated — expected call args включают link_preview=False (default), +test_edit_message_link_preview_false_passes_to_telethon, +test_edit_message_link_preview_true_passes_to_telethon, +test_edit_message_entities_link_preview_true_passes_to_telethon)
  - tests/test_url_parser.py — URL parser (9 тестов: public/private/comment/invalid/non-t.me/batch/index/is_valid)
  - tests/test_auth.py — session-based auth (8 тестов: login success/invalid, logout requires-auth/success, me requires-auth/after-login, session persists, concurrent sessions independent)
  - tests/test_api_channels.py — channels CRUD API (14 тестов: list empty/all/active_only, create success/duplicate 409/requires_auth, get by_id/not_found 404, update success/not_found 404/no_fields 422, delete success 204/not_found 404/cascade jobs+logs)
  - tests/test_link_replacer.py — link replacer (PR#66: +crossing boundary case 3a/3b + defensive validation drop invalid bounds 4 теста; PR#64: +_adjust_entity_offsets unit tests 7 + replace_link_in_post preserves bold/italic/multiple entities 4; PR#44: +entity URL replace, +text preferred over entity, +entity edit fails, +no match skip, +find entity url, +find skips empty text+entities; PR#86: +test_replace_link_in_post_link_preview_false_passes_to_edit, +test_replace_link_in_post_link_preview_true_passes_to_edit, +test_replace_link_in_post_link_preview_true_entity_path, +test_replace_link_in_post_defaults_to_link_preview_false)
  - tests/test_api_jobs.py — jobs API (PR#84: +test_list_jobs_total_includes_all_matching, +test_list_jobs_total_with_status_filter, +test_get_job_logs_total_includes_all_matching — фикс total через count_jobs/count_logs; PR#62: +test_replace_link_invalid_post_link_422, все payload'ы +post_link; PR#86: +test_replace_link_link_preview_true_reaches_runner (POST с link_preview:true → runner.submit(link_preview=True) через MagicMock(wraps=mock_runner.submit) spy), +test_replace_link_link_preview_defaults_to_false (без link_preview → runner.submit(link_preview=False)); PR#34: replace-link success/invalid-channel/invalid-pattern/limit-validation/default-limit/requires-auth, list 2-jobs/filter-by-channel, get by-id/not-found, logs with-logs/not-found, cancel success/already-done, jobs-endpoints-requires-auth; url-тесты удалены)
  - tests/test_api_patterns.py — PR#56: patterns + preview API (PR#62: +test_preview_replace_invalid_post_link_422, все payload'ы +post_link; PR#64: keep_tail→full_replace в payload'ах, +test_preview_replace_full_replace_true_appends_tail, rename test_preview_replace_keep_tail_strips_tail→test_preview_replace_partial_replace_keeps_tail, +test_replace_link_full_replace_true_appends_tail, +test_replace_link_partial_replace_keeps_pattern, test_replace_link_backward_compat_default_mode updated (default full_replace=True → pattern with .*); 18→21 тестов: patterns CRUD 8 — list empty/all/requires-auth, create success/duplicate-name/invalid-regex 422/requires-auth, delete success/403-builtin/404-absent; preview-replace 8 — success/empty/invalid-pattern 422/invalid-channel 404/requires-auth/text-vs-entity-match-source/compiled_pattern-in-response/full_replace_true_appends_tail; replace-link mode/full_replace 3 — simple-mode-compile/partial-replace-keeps-pattern/advanced-passes-raw + full_replace_true_appends_tail + backward_compat_default_mode). PR#60: list_patterns_empty→list_patterns_seeded (4 built-ins), create_pattern id=5 после seed, delete_custom total=4 (built-ins остаются), delete_builtin_403 использует seeded built-in
  - tests/test_pattern_compiler.py — PR#56: pattern compiler (PR#64: apply_keep_tail тесты удалены, compile_pattern тесты переписаны под full_replace; 14 тестов: compile_simple basic/multiple-wildcards/escape-special-chars/empty/no-wildcard, compile_pattern simple_default_full_replace_appends_tail/simple_without_full_replace_keeps_pattern/simple_with_trailing_wildcard_not_duplicated/simple_with_trailing_s_plus_not_appended/advanced_passthrough_default_full_replace/advanced_without_full_replace_passthrough/library_passthrough_with_full_replace/library_without_full_replace/default_full_replace_is_true/unknown_mode_raises)
  - tests/test_preview_replace.py — PR#56: preview dry-run (PR#64: +test_preview_replace_text_window_caps_at_50_chars_context, +test_preview_replace_short_post_shown_in_full, test_preview_replace_before_after_carry_link_only_for_text updated под 50-char context; 15 тестов: preview_replace basic/limit/empty/no-match, _preview_one text-path/entity-path/no-match, does-not-call-telethon, match_source-text-priority, total_matches-uncapped, entity-url-not-mutated, text_window_caps_at_50_chars_context, short_post_shown_in_full)
  - tests/test_models_pattern.py — PR#56: link_patterns CRUD (8 тестов: create+get, list_patterns, get_pattern_absent, delete_pattern_success, delete_pattern_builtin_raises_PermissionError, delete_pattern_absent_returns_false, create_pattern_6args, idx_link_patterns_name). PR#60: create_pattern id=5 (после 4 seeded built-ins), list_patterns len=6 (4 built-ins + 2 custom)
  - tests/test_seed_patterns.py — PR#60: seed built-in patterns (5 тестов: seed_inserts_four_builtin_patterns fresh DB→4 built-ins, seed_is_idempotent повторный→0 inserted, seed_preserves_custom_patterns is_builtin=0 не затирается, seed_does_not_collide_with_custom_name custom с тем же name не блокирует seed, seeded_builtin_cannot_be_deleted PermissionError)
  - tests/test_websocket.py — WebSocket + EventBus + JobRunner (8 тестов: ws-receives-events с auto-discovery PR#34, ws-requires-auth, ws-disconnect-unsubscribes, ws-publish-event-directly, EventBus subscribe/publish/unsubscribe, EventBus unsubscribe-idempotent, JobRunner start-idempotent, JobRunner cancel-returns-false-for-unknown)
  - tests/integration/__init__.py — package marker (PR#44)
  - tests/integration/test_real_edit.py — 4 integration tests (PR#44): find text url, find entity url, replace text url, replace entity url — opt-in via `pytest -m integration`, skipif no TELEGRAM_BOT_TOKEN/TELEGRAM_API_ID/TELEGRAM_API_HASH; PR#62: module-scoped `telegram_client` fixture (один client на все 4 теста через TELEGRAM_SESSION_STRING → no ImportBotAuthorizationRequest → no FloodWait, замена function-scoped `_client()` + `_reset_telegram_state` autouse)
  - web/src/tests/setup.ts — import @testing-library/svelte; afterEach(vi.restoreAllMocks)
  - web/src/tests/LayoutHarness.svelte — обёртка для +layout.svelte в тестах (передаёт data, рендерит child slot)
  - web/src/tests/login.test.ts — 3 теста: form render, submit+redirect, 401 error
  - web/src/tests/channels.test.ts — 9 тестов: 3 rows/empty/delete + 3 Add button + 3 ChannelForm; PR#38 — 3 селектора адаптированы под dual-layout (getByText→getAllByText+.length>0, getByRole→getAllByRole+[0])
  - web/src/tests/replace-link.test.ts — PR#58: 8 тестов (rewrite под 3-mode UI): disable submit when empty/new-link empty, enable submit when simple fields filled, limit validation, default limit 100, submit с mode/full_replace PR#64, full_replace=false when partial radio selected PR#64, defaults to full_replace=true (Полная замена radio checked) PR#64, Advanced mode switch. PR#62: +`disables submit when post link is empty`, все тесты с заполнением формы +`post_link: "https://t.me/test/140"` input, assertions replaceLink/previewReplace +post_link. PR#86: submit-тесты обновлены — form submit вызывает previewReplace (НЕ replaceLink напрямую), «Запустить button triggers previewReplace (preview-confirm-run flow)» (rename из «preview button calls previewReplace»), +`link_preview checkbox passes link_preview=true to previewReplace`, runNonce тесты updated — expected replaceLink call включает link_preview: false, +`runNonce passes link_preview=true when checkbox checked`. Mock `replaceLink` (НЕ `api.post` напрямую). PR#36: было 6 тестов (disabled/invalid-regex/limit/default/submit)
  - web/src/tests/jobs.test.ts — PR#84: +4 pagination теста (renders pagination controls when total > pageSize, does not render pagination when total <= pageSize, disables Prev on first page and Next on last page, fetches page 2 when Next is clicked); 5→9 тестов: render, cancel, WS progress, WS completed refetch, WS ignore other job_ids + pagination
  - web/src/tests/layout.test.ts — 3 теста: Channels nav, Logout button, username display
  - web/src/tests/api.test.ts — 2 теста: query serialization, ApiError on non-ok
  - web/playwright.config.ts — Playwright E2E config (PR#42): mobile project 375x812, baseURL docker-dind:8080, webServer off, workers 1
  - web/tests/e2e/helpers.ts — E2E helpers (PR#42): login(page), getSessionCookie(page), BASE_URL, TEST_CHANNEL_ID=2, TEST_USERNAME/TEST_PASSWORD
  - web/tests/e2e/mobile.spec.ts — 7 E2E тестов (PR#42): login, channels no duplicates, open button, replace-link form, job progress, zero-match, websocket
dependencies: [backend, frontend]
last_updated: 2026-07-22 (PR#86)
---

# tests — backend + frontend

## Backend tests (`tests/`)

```
tests/
├── __init__.py              # Пустой — package marker
├── conftest.py              # Fixtures: mock_settings (sync, monkeypatch env — SECRET_KEY 32+ chars, ADMIN_PASSWORD="secret", JOBS_MAX_CONCURRENCY/TELEGRAM_REQUEST_DELAY в delenv list; PR#62: MAX_PROBE_ID удалён из delenv list), mock_db (async, yields aiosqlite.Connection), create_channel/create_job (async factories, PEP 695 ChannelFactory/JobFactory), MockMessage dataclass (id/text/chat_id/message/date — Message-like, date поле для фильтра _fetch_messages_by_ids PR#32, entities: list[object] | None = None опциональное поле PR#44 для entity url тестов), mock_message, mock_telethon_client (patch TelegramClient конструктор, реальный get_client/start_client упражняется против mock, client.return_value=mock_channel_messages([]) по умолчанию PR#32), mock_telethon_get_message (monkeypatch core.telegram.get_message → AsyncMock(return_value=mock_message)), mock_telethon_edit_message (monkeypatch core.telegram.edit_message → AsyncMock(return_value=None)), mock_telethon_get_last_messages (PR#34 — monkeypatch core.telegram.get_last_messages → AsyncMock(return_value=[]), тесты переопределяют return_value), mock_runner (заменяет app.state.job_runner + app.state.event_bus на fresh in-memory, restore в teardown), authed_client (async, TestClient(app) as ctx mgr + POST /api/auth/login → залогиненная сессия). Хелперы _install_runner/_restore_default_runner (media-gen паттерн). mock_channel_messages(messages) helper (PR#32) — создаёт MagicMock с .messages list (эмулирует ChannelMessages результат, НЕ AsyncMock т.к. plain object)
├── test_health.py           # TestClient → GET /health → 200 {"status":"ok"}
├── test_config.py           # 3 теста: defaults (env не задан), custom env (ADMIN_USERNAME и др.), frozen check (FrozenInstanceError). PR#62: MAX_PROBE_ID удалён из env list + assertion settings.max_probe_id
├── test_db.py               # 6 тестов: init_db_creates_file, init_db_idempotent, pragmas_set (WAL+foreign_keys), schema_created (sqlite_master), get_db_yields_connection, close_db_noop_when_uninitialized
├── test_models_channel.py  # 7 тестов: create+get, get_by_telegram_id, list_active_only, update (title/username/is_active), update_rejects_unknown_field (ValueError), delete, unique_telegram_id_constraint (IntegrityError)
├── test_models_job.py       # PR#84: +count_jobs 4 теста (all/filter-by-channel/filter-by-status/empty). 9 тестов: create+get, list_filter_by_channel, list_filter_by_status, update_job_status, delete_job_cascade_logs (FK CASCADE), count_jobs_all, count_jobs_filter_by_channel, count_jobs_filter_by_status, count_jobs_empty
├── test_models_log.py       # 4 теста: create+list, list_limit_offset (pagination), delete_logs (returns count), delete_channel_cascade_jobs_logs (FK CASCADE channel→jobs→logs)
├── test_telegram_client.py  # PR#62: 20 тестов (удалены 5 _find_max_id тестов, добавлены 3 get_last_messages с max_id + 4 parse_post_link теста). PR#44: 22 теста: get_client_singleton, start_client_idempotent (await_count), get_message_by_id, get_message_returns_none_for_missing, get_messages_batch_filters_none, edit_message_success (PR#64: edit_message вызывается с formatting_entities=None kwarg), edit_message_propagates_error (BadRequestError→RPCError), resolve_entity, get_bot_info, stop_client_resets_state + 9 PR#32: test_get_last_messages_success (mock _find_max_id=150, range fetch 100, проверка ChannelsGetMessagesRequest.id), test_get_last_messages_empty_channel (_find_max_id=0 → []), test_get_last_messages_limit_larger_than_max_id (max_id=50, limit=100 → ids=range(50,0,-1)), test_find_max_id_binary_search (side_effect по request.id[0], existing={1250,1875} → 1875), test_find_max_id_all_empty (все probes [None] → 0), test_fetch_messages_by_ids_filters_empty ([msg1,None,msg2,date_none] → [msg1,msg2]), test_flood_wait_retry (FloodWaitError(capture=2) первый раз, потом OK, asyncio.sleep(3) 1 раз), test_flood_wait_max_retries_exceeded (всегда FloodWaitError → re-raise после 3, await_count==3, flood-sleeps==2), test_delay_between_requests (asyncio.sleep с delay параметром PR#34, НЕ Settings.from_env() внутри _find_max_id) + 1 PR#34: test_get_last_messages_reads_settings_once (monkeypatch Settings.from_env с counter, stub start_client/_get_channel_input чтобы их from_env вызовы не pollute count, assert from_env_calls == 1) + 1 PR#44: test_edit_message_entities_invokes_raw_api (monkeypatch _get_channel_input → MagicMock(channel_id, access_hash), assert MessagesEditMessageRequest с InputPeerChannel peer, id, message, entities переданы корректно). PR#62: test_get_last_messages_with_max_id, test_get_last_messages_max_id_zero_returns_empty, test_get_last_messages_limit_larger_than_max_id (переписан под max_id параметр), test_parse_post_link_public_channel_url, test_parse_post_link_private_channel_url, test_parse_post_link_plain_number, test_parse_post_link_invalid_raises_value_error
├── test_url_parser.py       # 9 тестов: public channel, private channel (-100<internal_id>), ?comment= query, invalid format, non-t.me domain, private non-digit, batch, batch with index, is_valid_post_url true/false
├── test_auth.py             # 8 тестов: login_success, login_invalid_credentials, logout_requires_auth, logout_success, me_requires_auth, me_after_login, session_persists_across_requests, concurrent_sessions_independent
├── test_api_channels.py     # 14 тестов: list_channels_empty, list_channels_returns_all, list_channels_active_only, create_channel_success, create_channel_duplicate_telegram_id (409), create_channel_requires_auth (401, отдельный TestClient без authed_client), get_channel_by_id, get_channel_not_found (404), update_channel_success, update_channel_not_found (404), update_channel_no_fields (422), delete_channel_success (204), delete_channel_not_found (404), delete_channel_cascade_jobs_logs (FK CASCADE через db_handle fixture)
├── test_link_replacer.py    # PR#64: +7 _adjust_entity_offsets unit tests (no_matches_returns_copies, delta_positive, delta_negative, delta_zero, match_inside_entity_grows_length, does_not_mutate_originals, empty_entities) + 4 replace_link_in_post preserves entity tests (bold/italic/no_entities_passes_none/multiple_entities). PR#44: 21 тест: replace_link_basic, replace_link_multiple, replace_link_no_match, replace_link_invalid_pattern_raises, validate_pattern_valid, validate_pattern_invalid_raises, replace_link_in_post_success, replace_link_in_post_message_not_found, replace_link_in_post_no_replacements, replace_link_in_post_edit_fails, replace_link_in_post_empty_text + 4 PR#34: test_find_posts_with_pattern_filters_matching (4 messages: matching/non-match/empty/None-text → только matching), test_find_posts_with_pattern_no_matches (empty list), test_replace_link_in_posts_summary (orchestrator с 3 messages 2 matching + 1 non-match, _on_progress callback, asserts {total:3, edited:2, failed:0, skipped:1} + progress_calls[-1]==(2,0,3)), test_replace_link_in_posts_without_progress_callback (orchestrator без callback работает) + 6 PR#44: test_replace_link_in_post_replaces_entity_url (entity url replaced через edit_message_entities, MessageEntityTextUrl rebuilt с new_link, offset/length preserved), test_replace_link_in_post_prefers_text_match_over_entity (pattern matches both text+entity → text path wins, edit_message called, edit_message_entities NOT called), test_replace_link_in_post_entity_edit_fails (edit_message_entities raises → success=False с error), test_replace_link_in_post_skips_when_no_match_in_text_or_entities (no match in text or entity urls → skipped, no edit called), test_find_posts_with_pattern_matches_entity_url (entity url матчится, msg_plain без entities не матчит), test_find_posts_with_pattern_skips_when_text_and_entities_empty (no text + no entities → never matched)
├── test_api_jobs.py         # PR#84: +test_list_jobs_total_includes_all_matching (15 jobs, limit=10 → total=15, len(jobs)=10), +test_list_jobs_total_with_status_filter (total уважает status-фильтр), +test_get_job_logs_total_includes_all_matching (5 logs, limit=2 → total=5, len(logs)=2). PR#62: +test_replace_link_invalid_post_link_422, все payload'ы +post_link. PR#34: 15 тестов: replace_link_success (body {pattern, new_link, limit:100}, mock get_last_messages возвращает 3 matching + 1 non-match), replace_link_invalid_channel (404), replace_link_invalid_pattern (422), replace_link_limit_validation (NEW PR#34: limit=0→422, limit=1001→422, limit=100→201), replace_link_default_limit (NEW PR#34: body без limit → default 100, poll job до terminal, assert get_last_messages awaited once с called_limit==100), replace_link_requires_auth, list_jobs (2 jobs), list_jobs_filter_by_channel, get_job_by_id, get_job_not_found, get_job_logs, get_job_logs_job_not_found, cancel_job_success, cancel_job_already_done, jobs_endpoints_require_auth. УДАЛЕНЫ PR#34: test_replace_link_invalid_url, test_replace_link_url_wrong_channel (URL-валидации больше нет). Хелперы PR#34: _matching_messages(link, count) принимает literal URL (НЕ regex), _drain_until_settled(client, job_id, rounds=50) poll'ит GET /api/jobs/{id} пока terminal + GET /health между polls (pump asyncio loop для bg task)
├── test_api_patterns.py     # PR#62: +test_preview_replace_invalid_post_link_422, все payload'ы +post_link. PR#64: keep_tail→full_replace в payload'ах, +test_preview_replace_full_replace_true_appends_tail, rename test_preview_replace_keep_tail_strips_tail→test_preview_replace_partial_replace_keeps_tail, +test_replace_link_full_replace_true_appends_tail, +test_replace_link_partial_replace_keeps_pattern, test_replace_link_backward_compat_default_mode updated (default full_replace=True → pattern with .*). PR#56: 18 тестов. Patterns CRUD (8): list_patterns_empty, list_patterns_returns_all, list_patterns_requires_auth, create_pattern_success (201, is_builtin=0), create_pattern_invalid_regex (422), create_pattern_requires_auth, delete_pattern_success (204), delete_pattern_builtin_returns_403, delete_pattern_absent_returns_404. Preview-replace (7): preview_replace_success (3 before→after pairs, compiled_pattern in response), preview_replace_empty_channel, preview_replace_invalid_pattern (422), preview_replace_invalid_channel (404), preview_replace_requires_auth, preview_replace_text_vs_entity_match_source (text path wins), preview_replace_compiled_pattern_in_response. Replace-link mode/full_replace (PR#64: renamed from keep_tail): replace_link_simple_mode_compiles_wildcard, replace_link_partial_replace_keeps_pattern (was keep_tail_strips_suffix), replace_link_advanced_passes_raw + full_replace_true_appends_tail + backward_compat_default_mode. Локальный db_handle fixture (не в conftest, т.к. test_api_jobs.py уже имеет свой). PR#60: list_patterns_empty→list_patterns_seeded (4 built-ins, names check), create_pattern id=5 после seed (total=5, patterns[0].is_builtin=True), delete_pattern_success total=4 после delete (built-ins остаются), delete_pattern_builtin_returns_403 использует seeded built-in (через GET /api/patterns, не создаёт свой)
├── test_pattern_compiler.py # PR#64: apply_keep_tail тесты удалены, compile_pattern тесты переписаны под full_replace. PR#56: 14 тестов (было 20). compile_simple (5): basic (*→.*), multiple_wildcards, escape_special_chars (re.escape экранирует `-` в Python 3.12+), empty_string, no_wildcard. compile_pattern (9): simple_default_full_replace_appends_tail, simple_without_full_replace_keeps_pattern, simple_with_trailing_wildcard_not_duplicated, simple_with_trailing_s_plus_not_appended, advanced_passthrough_default_full_replace, advanced_without_full_replace_passthrough, library_passthrough_with_full_replace, library_without_full_replace, default_full_replace_is_true, unknown_mode_raises_ValueError
├── test_preview_replace.py # PR#64: +test_preview_replace_text_window_caps_at_50_chars_context, +test_preview_replace_short_post_shown_in_full, test_preview_replace_before_after_carry_link_only_for_text updated под 50-char context. PR#56: 15 тестов (было 13). preview_replace (4): basic (3 pairs, total_matches), limit (cap previews), empty_messages, no_match. _preview_one (3): text_path (before/after = 50-char context window PR#64), entity_path (before/after = только url), no_match_returns_None. Dry-run safety (8): does_not_call_telethon (edit_message/edit_message_entities NOT called), match_source_text_priority (text+entity match → text wins), total_matches_uncapped (limit=3 но total_matches=5), entity_url_not_mutated (preview НЕ меняет entity.url, реальный edit меняет), preview_replace_with_keep_tail (renamed/updated под full_replace), preview_replace_simple_mode, text_window_caps_at_50_chars_context, short_post_shown_in_full
├── test_models_pattern.py  # PR#56: 8 тестов. create_pattern_and_get (insert + fetch by id), list_patterns (ORDER BY id), get_pattern_absent_returns_None, delete_pattern_success_returns_True, delete_pattern_builtin_raises_PermissionError (is_builtin=1), delete_pattern_absent_returns_False, create_pattern_6args (PLR0913 noqa), idx_link_patterns_name (index created). PR#60: create_pattern id=5 (после 4 seeded built-ins), list_patterns len=6 (4 built-ins + 2 custom, проверка rows[-2]/rows[-1])
├── test_seed_patterns.py   # PR#60: 5 тестов. test_seed_inserts_four_builtin_patterns (fresh DB → 4 built-ins), test_seed_is_idempotent (повторный seed_builtin_patterns → 0 inserted), test_seed_preserves_custom_patterns (custom is_builtin=0 не затирается), test_seed_does_not_collide_with_custom_name (custom с тем же name не блокирует seed, т.к. проверяется is_builtin=1), test_seeded_builtin_cannot_be_deleted (seeded builtin → PermissionError)
├── test_websocket.py        # 8 тестов: ws_requires_auth (anon_ws_client → close 4001), ws_receives_events (PR#34: body {pattern, new_link, limit}, mock get_last_messages возвращает 1 matching MockMessage с literal URL в text, asserts job_started/progress/completed), ws_disconnect_unsubscribes, ws_publish_event_directly, test_event_bus_subscribe_publish_unsubscribe, test_event_bus_unsubscribe_idempotent, test_runner_start_idempotent, test_runner_cancel_returns_false_for_unknown_job
└── integration/             # PR#44 — integration tests against real Telegram channel; PR#62 — module-scoped telegram_client fixture (FloodWait fix)
    ├── __init__.py          # Пустой — package marker
    └── test_real_edit.py    # 4 теста (pytestmark: integration marker + skipif no creds): test_find_text_url (URL в raw text матчит), test_find_entity_url (URL в entity матчит после fix), test_replace_text_url (replace URL в raw text, post отредактирован), test_replace_entity_url (replace URL в entity, entity.url обновлён). PR#62: module-scoped `telegram_client` fixture (один client на все 4 теста через TELEGRAM_SESSION_STRING → no ImportBotAuthorizationRequest → no FloodWait, замена function-scoped `_client()` + `_reset_telegram_state` autouse). Fixtures test_text_url_msg/test_entity_url_msg: function-scoped, send test message to CHANNEL_ID (env TELESOFT_TEST_CHANNEL_ID, default -1003903711726), yield, delete on teardown (finally блок, без disconnect — module fixture disconnect'ит в конце). Opt-in: `pytest -m integration`, skipif no TELEGRAM_BOT_TOKEN/TELEGRAM_API_ID/TELEGRAM_API_HASH → skip в CI
```

### Patterns

- **pytest-asyncio** (asyncio_mode=auto) — async-тесты без `@pytest.mark.asyncio`
- **Coverage gate 80%** — `--cov=telesoft --cov-fail-under=80` (текущее покрытие 94.08% после PR #22)
- **TestClient** (starlette) требует httpx — добавлен в dev-deps
- **conftest fixtures** — `mock_settings` (monkeypatch env vars, SECRET_KEY 32+ chars для SessionMiddleware, ADMIN_PASSWORD="secret", `JOBS_MAX_CONCURRENCY` в delenv list; PR#62: `MAX_PROBE_ID` удалён из delenv list), `mock_db` (async, `monkeypatch.setenv("DB_PATH", tmp_path/telesoft.db)`, yields `aiosqlite.Connection` для прямых запросов к моделям), `create_channel`/`create_job` (async factories с дефолтами), `MockMessage` dataclass (id/text/chat_id/message — Message-like), `mock_message`, `mock_telethon_client` (patch `TelegramClient` конструктор → AsyncMock с get_messages/edit_message/get_entity/get_me/start/disconnect; реальный `get_client`/`start_client` упражняется против mock; reset `_state` в setup/teardown), `mock_telethon_get_message` (monkeypatch `core.telegram.get_message` → AsyncMock(return_value=mock_message); module-level patch, виден всем importer'ам `link_replacer`), `mock_telethon_edit_message` (monkeypatch `core.telegram.edit_message` → AsyncMock(return_value=None); return_value=None OK — `link_replacer` игнорирует return), `mock_runner` (заменяет `app.state.job_runner` + `app.state.event_bus` на fresh in-memory `JobRunner(max_concurrency=2)` + `EventBus`, restore prev в teardown), `authed_client` (async, TestClient(app) as ctx mgr + POST /api/auth/login → залогиненная сессия)
- **PEP 695 type aliases** для фикстур — `type ChannelFactory = Callable[..., Awaitable[ChannelRow]]`, `type JobFactory = ...`
- **per-file-ignores** для tests: S104 (`0.0.0.0`), S105 (secret_key/token assignment)
- **FK CASCADE тесты** — `test_delete_job_cascade_logs`, `test_delete_channel_cascade_jobs_logs` проверяют каскадное удаление (требуют `PRAGMA foreign_keys=ON`)
- **Telethon mock design** — `mock_telethon_client` patch'ит `TelegramClient` конструктор (НЕ `get_client`/`start_client`), что позволяет тестировать singleton + idempotency + locking против mock client. `BadRequestError(request=None, message="...")` для эмуляции `RPCError` (конструктор требует `request`)
- **`mock_channel_messages` helper** (PR#32) — `mock_channel_messages(messages: list[object | None]) -> AsyncMock` создаёт `MagicMock` с `.messages` list (эмулирует `ChannelMessages` результат raw API `channels.GetMessagesRequest`). `MagicMock` (НЕ `AsyncMock`) т.к. `ChannelMessages` — plain object, не coroutine. Тесты кладут `[None]`, `[msg1, None, msg2]`, и т.д. В `mock_telethon_client` fixture: `client.return_value = mock_channel_messages([])`, `client.side_effect = None` (по умолчанию пустой, тесты переопределяют через `side_effect` для разных probes или `return_value` для одинакового ответа)
- **`MockMessage.date` поле** (PR#32) — добавлено для фильтра в `_fetch_messages_by_ids` (`m is not None and getattr(m, "date", None) is not None`). `date: object = None` (по умолчанию None — эмулирует служебное сообщение без даты). Тесты могут передавать `"2026-07-20"` (строка-дата) или `None`
- **`FloodWaitError(capture=N)` в тестах** (PR#32) — `FloodWaitError(request=None, capture=N)` → `e.seconds == N`. `capture` это positional arg в `RPCError.__init__`, `seconds` вычисляется из message. mypy: `# type: ignore[call-arg]` т.к. `capture` не в telethon stubs. `test_flood_wait_retry`/`test_flood_wait_max_retries_exceeded` проверяют bounded retry (max 3) в `_fetch_messages_by_ids`
- **`monkeypatch.setattr(telegram_module.asyncio, "sleep", AsyncMock())`** (PR#32) — patch'ит `asyncio.sleep` в модуле `core.telegram` для тестов delay/retry (НЕ глобальный asyncio.sleep). Позволяет assert'ить `await_args_list` (количество вызовов, args). `test_delay_between_requests` (PR#34 updated) проверяет `c.args[0] == delay` (параметр, НЕ `settings.telegram_request_delay` — delay теперь параметр в `_find_max_id`), `test_flood_wait_retry` — `c.args[0] == 3` (exc.seconds + 1 = 2 + 1). PR#62: `_find_max_id` удалён, `test_delay_between_requests` удалён, delay тестируется только через `test_flood_wait_retry` (`exc.seconds + 1`)
- **`test_get_last_messages_reads_settings_once`** (PR#34, removed PR#62) — ранее monkeypatch `Settings.from_env` с counter, stub `start_client`/`_get_channel_input`, assert `from_env_calls == 1`. PR#62: тест удалён — `get_last_messages` больше не вызывает `Settings.from_env()` (binary search `_find_max_id` удалён, `max_id` передаётся caller'ом, delay не используется в `get_last_messages`)
- **Module-scoped `telegram_client` fixture** (PR#62) — integration tests: один `TelegramClient` на все 4 теста в модуле (scope="module"), использует `TELEGRAM_SESSION_STRING` (saved auth) → `client.start()` НЕ вызывает `ImportBotAuthorizationRequest` → no FloodWait. Заменяет function-scoped `_client()` (каждый тест создавал новый client → FloodWait 897 сек) + `_reset_telegram_state` autouse fixture. pytest-asyncio `auto` mode поддерживает async fixtures с `scope="module"`. Teardown: `delete_messages` per message fixture, `disconnect` в module fixture teardown
- **Auth test design** (`test_auth.py`) — `client` fixture: monkeypatch `SECRET_KEY` (32+ chars), `ADMIN_USERNAME`, `ADMIN_PASSWORD`, `DB_PATH=tmp`, `TestClient(app)` as context manager (trigger lifespan). Session cookies через `TestClient` (stateful). Concurrent sessions — два отдельных `TestClient` (независимые cookies). `test_concurrent_sessions_independent` проверяет, что logout одного клиента не влияет на другого
- **`authed_client` fixture** (`conftest.py`, async) — аналог `client` из `test_auth.py`, но с предзаполненной сессией: monkeypatch env vars → `TestClient(app)` as context manager (trigger lifespan → init_db) → `POST /api/auth/login` с admin/secret → assert 200 → yield client. Используется в `test_api_channels.py` (13 из 14 тестов) и `test_api_jobs.py`. `test_create_channel_requires_auth`/`test_replace_link_requires_auth`/`test_jobs_endpoints_require_auth` используют отдельный TestClient без `authed_client` для проверки 401. pytest-asyncio `auto` mode поддерживает async fixtures без декоратора, работает в sync- и async-тестах
- **`db_handle` fixture** (`test_api_channels.py`) — exposes shared app DB connection (`connection._state.db`) для прямых SQL-запросов в cascade-тесте (`test_delete_channel_cascade_jobs_logs` создаёт job+log через model functions напрямую). Приватный API, но `connection` module стабилен (PR#12)
- **`mock_telethon_get_message` / `mock_telethon_edit_message` — module-level monkeypatch** — `monkeypatch.setattr(telegram_module, "get_message", AsyncMock)`. Это заменяет функцию в модуле `core.telegram`, так что `link_replacer.py` (который импортирует `telegram_module` и вызывает `telegram_module.get_message`) видит mock. НЕ нужно patch'ить `core.link_replacer.telegram_module` — `link_replacer` использует `from telesoft.core import telegram as telegram_module`, monkeypatch на `telegram_module.get_message` меняет атрибут модуля, виден всем importer'ам
- **`mock_runner` fixture** — НЕ isolates test from real runner: `mock_runner` создаёт real `JobRunner` с fresh `EventBus`, но `submit` запускает real `_run_job` (которая вызывает `replace_link_in_post` → Telethon mock). Если нужен no-op runner — override `submit` в тесте или используй `_install_runner` с custom work_fn (media-gen паттерн). Для API tests (только 201 + job_id check) real runner OK — Telethon mock'и возвращают success
- **WebSocket test design** (`test_websocket.py`) — `ws_client`/`anon_ws_client` fixtures (TestClient with `websocket_connect("/api/ws")`). `test_ws_receives_events` — `mock_runner` + `mock_telethon_get_message`/`mock_telethon_edit_message`, submit job via runner, receive events via WS, assert `job_started`/`progress`/`completed`. `test_ws_requires_auth` — `anon_ws_client` (без login) → close code 4001. EventBus unit tests — direct `EventBus()` instance. JobRunner unit tests — direct `JobRunner()` instance (start_idempotent, cancel_returns_false_for_unknown)
- **`mock_telethon_get_last_messages` fixture** (PR#34) — `AsyncMock(return_value=[])`, monkeypatch `telegram_module.get_last_messages`. Тесты переопределяют `return_value` для конкретных сценариев (e.g. `test_replace_link_success` — 3 matching + 1 non-match MockMessage). Заменяет ручной monkeypatch в каждом тесте — DRY для auto-discovery тестов
- **`_matching_messages(link, count)` helper** (PR#34) — принимает literal URL (НЕ regex-строку с `\.`), embeds в text как `f"see {link} here"`. Раньше принимал regex-строку → text содержал `\.` буквально → regex не матчит. Pattern передаётся отдельно в body. Используется в `test_replace_link_success`, `test_replace_link_limit_validation`, `test_replace_link_default_limit`, `test_ws_receives_events`
- **`_drain_until_settled(client, job_id, rounds=50)` helper** (PR#34) — sync `TestClient` НЕ pump'ит asyncio event loop автоматически между requests. Background task (`runner.submit` → `asyncio.create_task`) может не успеть выполниться к моменту assert. Helper: poll `GET /api/jobs/{id}` пока status terminal + `GET /health` между polls (pump loop). Без этого `mock_telethon_get_last_messages.assert_awaited_once()` падает (0 awaits — bg task не успел)
- **Coverage 94.69%** после PR #62 (gate 80% пройден). PR#64: покрытие сохранено/увеличено за счёт новых тестов _adjust_entity_offsets (7 unit) + replace_link_in_post preserves entity (4) + full_replace tests в test_api_patterns/test_pattern_compiler. Uncovered: `core/runner.py` 80% (defensive `assert sem is not None`, `is_running`/`get_task`/`active_count`, exception path в `_mark_final` без error column), `api/routers/jobs.py` 95% (defensive `assert row is not None` после `_get_job_or_404`, username match branch), `schemas/job.py` 94% (`WsEvent.from_event` — tested via integration, не unit). PR#64: 191→202 backend тестов (+11: 7 _adjust_entity_offsets + 4 replace_link_in_post preserves entity; test_pattern_compiler 20→14 net -6; test_api_patterns +3 net; test_preview_replace +2 net; test_telegram_client 0 net — assertion update only). Frontend 36→37 тестов (+1: defaults to full_replace=true)

## Integration tests (`tests/integration/`) — PR#44

```
tests/integration/
├── __init__.py           # Пустой — package marker
└── test_real_edit.py     # 4 теста против реального Telegram channel (opt-in)
```

### Patterns

- **Opt-in `pytest -m integration` + skipif no creds** (PR#44) — `pytestmark = [pytest.mark.integration, pytest.mark.skipif(not CREDS, reason="...")]`. Integration tests НЕ в CI (skipif no creds → skip автоматически, не падают). Локальный запуск: `pytest -m integration` (или `pytest tests/integration/`). Главный агент прогоняет через `docker exec telesoft-api-1 pytest -m integration` (нужен running контейнер с creds + test channel access)
- **`pyproject.toml` markers** (PR#44) — `[tool.pytest.ini_options] markers = ["integration: requires Telegram creds + real channel (opt-in via -m integration)"]`. `testpaths = ["tests"]` включает `tests/integration/` без доп. настройки. Coverage gate `--cov-fail-under=80` не понижается (integration tests skipif no creds, не влияют на coverage)
- **`TELESOFT_TEST_CHANNEL_ID` env override** (PR#44) — default `-1003903711726` (test channel из spike PR#30). Через env var можно указать другой channel для integration tests (не хардкод)
- **Fixtures cleanup в `finally`** (PR#44, PR#62) — `test_text_url_msg`/`test_entity_url_msg` fixtures: function-scoped, `client.send_message(CHANNEL_ID, ...)` → `try: yield msg` → `finally: await client.delete_messages(CHANNEL_ID, [msg.id])` (без `disconnect` — module fixture disconnect'ит в конце PR#62). Даже если test падает, message удаляется. Если test runner умирает mid-test, message может остаться (требуется ручная очистка)
- **`parse_mode="md"` для entity url fixture** (PR#44) — `client.send_message(CHANNEL_ID, "Test post [click here](https://new.example.com/path) end", link_preview=False, parse_mode="md")` создаёт `MessageEntityTextUrl` entity (formatted link, URL скрыт в `entity.url`). `link_preview=False` — отключает link preview (не нужен для test)
- **4 теста покрывают find+replace для text+entity** (PR#44) — `test_find_text_url` (URL в raw text матчит), `test_find_entity_url` (URL в entity матчит после fix), `test_replace_text_url` (replace URL в raw text, post отредактирован, assert new URL в `edited.text`), `test_replace_entity_url` (replace URL в entity, entity.url обновлён, assert new URL в `[e.url for e in edited.entities if hasattr(e, "url")]`). PR#62: тесты используют `telegram_client.get_messages(CHANNEL_ID, ids=[msg.id])` напрямую (high-level API by-ID fetch, вместо `telegram_module.get_message` singleton). Pattern `r"https://new\.example\.com/path"`, NEW_LINK `"https://new.example.com/edited"`
- **Integration tests доказывают raw API работает** (PR#44) — `messages.EditMessageRequest` (entity path) НЕ был в spike PR#30 (только `channels.GetMessagesRequest`). Integration tests подтверждают что raw API edit entities работает на реальном Telegram для bot-admin

## Smoke test (НЕ часть pytest suite)

`scripts/smoke_test.py` (PR #28, issue #27) — standalone end-to-end smoke test, **НЕ** входит в pytest suite. Запускается отдельно через `uv run python scripts/smoke_test.py`, требует запущенный backend (Docker compose или локальный uvicorn). Проверяет API plumbing: login → list channels → create channel → get by id → delete. НЕ запускает replace-link (нужны реальные post URLs + подключенный бот). Подробности — см. [scripts.md](scripts.md).

## Frontend tests (`web/src/tests/`)

```
web/src/tests/
├── setup.ts              # import "@testing-library/svelte"; afterEach(vi.restoreAllMocks) — очистка моков между тестами
├── LayoutHarness.svelte # обёртка для +layout.svelte (передаёт data prop, рендерит child slot)
├── login.test.ts         # 3 теста: form render (username/password fields), submit+redirect (POST /api/auth/login → goto), 401 error display
├── channels.test.ts      # 9 тестов: 3 rows render (title+active badge)/empty state/delete action + 3 Add button (open form/submit+refresh/cancel) + 3 ChannelForm (disabled when empty/enabled when filled/calls onSaved after POST); PR#38 — 3 селектора адаптированы под dual-layout (getByText→getAllByText, getByRole→getAllByRole[0])
    ├── replace-link.test.ts  # PR#58: 8 тестов (rewrite под 3-mode UI): disable submit when empty/new-link empty, enable submit when simple fields filled, limit validation, default limit 100, submit с mode/full_replace PR#64, full_replace=false when partial radio selected PR#64, defaults to full_replace=true (Полная замена radio checked) PR#64, Advanced mode switch. PR#62: +`disables submit when post link is empty`, все тесты с заполнением формы +post_link input, assertions +post_link. PR#86: submit-тесты обновлены — form submit вызывает previewReplace (НЕ replaceLink напрямую), «Запустить button triggers previewReplace (preview-confirm-run flow)» (rename), +`link_preview checkbox passes link_preview=true to previewReplace`, runNonce тесты updated — expected replaceLink call включает link_preview: false, +`runNonce passes link_preview=true when checkbox checked`. Mock `replaceLink` (НЕ `api.post` напрямую). PR#36: было 6 тестов (disabled/invalid-regex/limit/default/submit). УДАЛЁН "parses textarea URLs into an array and submits" (textarea убран PR#36)
├── jobs.test.ts          # PR#84: +4 pagination теста (renders pagination controls when total > pageSize, does not render pagination when total <= pageSize, disables Prev on first page and Next on last page, fetches page 2 when Next is clicked). 9 тестов: renders header/status/progress/logs, cancel button calls POST /api/jobs/{id}/cancel, WS progress event updates progress, WS completed event refetches logs, WS events for other job_ids ignored + pagination
├── layout.test.ts        # 3 теста: Channels nav item, Logout button, signed-in username display
└── api.test.ts           # 2 теста: query serialization (URLSearchParams, undefined пропущен), ApiError on non-ok response
```

### Patterns

- **Vitest + jsdom** — DOM-окружение для компонентных тестов
- **@testing-library/svelte** — рендеринг Svelte-компонентов в тестах (import в setup.ts)
- **coverage v8** — через @vitest/coverage-v8
- **Mocking через `vi.hoisted` + `vi.mock`** — mock `../lib/api` (api.post/del), `$app/navigation` goto, `$app/state` page
- **`mockImplementation` для fresh Response** — `vi.fn().mockResolvedValue(response)` возвращает тот же Response → "Body is unusable" на втором вызове. Использовать `mockImplementation(() => Promise.resolve(jsonResponse(...)))` для fresh Response каждый вызов
- **LayoutHarness.svelte** — обёртка для рендера `+layout.svelte` в тестах (передаёт `data` prop, рендерит child slot через `{@render children()}`)
- **WebSocket mock pattern** (PR#26, `jobs.test.ts`) — `vi.mock("../lib/ws", ...)` с class mock. `onMessage(handler)` регистрирует handler в `Set<WsMessageHandler>` (НЕ static `vi.fn()`), `emitWsEvent(type, data)` helper дёргает все handlers. `beforeEach` очищает `onMessageHandlers` Set между тестами. `connect()`/`close()` — `vi.fn()` no-op. Позволяет тестировать WS event handling без реального соединения.
- **36→37 тестов, 6 файлов** (PR#64, было 36 в PR#62; PR#84: jobs 5→9 +4 pagination теста → 41 тестов) — login (3), channels (9), replace-link (10, PR#64 +`defaults to full_replace=true (Полная замена radio checked)` + rename `sends keep_tail=true when checkbox is checked`→`sends full_replace=false when partial radio is selected`), jobs (9, PR#84 +4 pagination), layout (3), api (2). Все зелёные. Smoke-тест из PR#2 удалён (реальные тесты заменяют)
- **`getAllByText`/`getAllByRole` для dual-layout** (PR#38) — dual-layout pattern `hidden sm:block` + `sm:hidden` рендерит текст дважды (table + cards) в DOM. В jsdom (Vitest) media queries НЕ оцениваются → оба блока видимы → `getByText` падает на "Found multiple elements". Решение: `getAllByText("alpha").length > 0` / `getAllByRole("button", { name: "Delete" })[0]`. Альтернатива — mock `window.matchMedia` в vitest setup, но усложняет тесты и ломает text-based селекторы. `getAllBy*` проще и stable. Применено в `channels.test.ts` (3 теста). `jobs.test.ts` — БЕЗ изменений (message_id уникален per log, `getByText` находит первый match).
- **Vitest НЕ имеет coverage gate** (в отличие от backend pytest `--cov-fail-under=80`). `+page.ts`/`+layout.ts` load functions не покрыты (требуют SvelteKit load context). `ws.ts` покрыт косвенно через mock в `jobs.test.ts`.

## E2E tests (`web/tests/e2e/`) — PR#42

```
web/
├── playwright.config.ts  # E2E config: testDir ./tests/e2e, timeout 30s, retries 1, fullyParallel false, workers 1 (sequential), baseURL http://docker-dind:8080 (nginx, контейнеры running, webServer отключён), screenshot/video/trace retain-on-failure, project "mobile" (viewport 375x812, deviceScaleFactor 2, isMobile true, hasTouch true — iPhone SE-like)
└── tests/e2e/
    ├── helpers.ts        # BASE_URL, TEST_CHANNEL_ID=2 (реальный test channel telegram_id=-1003903711726 из spike PR#30), TEST_USERNAME/TEST_PASSWORD (admin/admin из .env контейнера api), login(page, username, password) — goto /login, fill #username/#password, click submit, waitForURL **/channels; getSessionCookie(page) — читает cookie "session" из context
    └── mobile.spec.ts    # 7 тестов, beforeEach login (каждый изолирован, новый context): 1) login flow redirects to channels, 2) channels list no duplicates on mobile (table hidden, cards visible, ≥1 card), 3) open button on channel card navigates to detail, 4) replace-link form submission redirects to job detail (несуществующий pattern → zero-match), 5) job detail shows progress bar and reaches terminal status (done|failed|cancelled, timeout 30s), 6) zero-match feedback shows amber alert when total is zero, 7) websocket connection establishes without console errors (listener + filter по "websocket"/"ws"/"socket")
```

### Patterns

- **Playwright** (`@playwright/test`) — Microsoft E2E framework, auto-waiting, tracing, mobile emulation. `npx playwright install chromium --with-deps` — Chromium 149 + FFmpeg + Headless Shell в `~/.cache/ms-playwright/`
- **Mobile-first** — единственный project `mobile` (375x812, iPhone SE-like). Desktop/tablet — follow-up
- **Running контейнеры + webServer отключён** — тесты запускаются против уже running docker compose (nginx + api + web), `baseURL: docker-dind:8080`. Тесты НЕ запускают/останавливают контейнеры. `localhost:8080` НЕ работает из opencode container (nginx проброшен на DinD host)
- **Sequential `workers: 1`, `fullyParallel: false`** — общий running backend → race conditions при параллельности (test 4 создаёт job, test 5 видит чужой job). Sequential гарантирует изоляцию
- **`TEST_CHANNEL_ID = 2` hardcode** — реальный test channel (telegram_id=-1003903711726 из spike PR#30). Channel id=1 — фейковый (-1001234567890), replace-link падает. Channel id=2 с несуществующим pattern → done, total=0 (zero-match для test 6)
- **CSS class-based селекторы** — `div.hidden.overflow-x-auto.sm\:block` (table wrapper), `div.space-y-3.sm\:hidden` (cards section), `#rl-pattern`/`#rl-new-link`/`#rl-limit` (form inputs), `.border-amber-900.bg-amber-950` (zero-match alert). `div.` prefix + `overflow-x-auto` для уникальности (`.hidden.sm\:block` матчит 2 элемента — span "Signed in as admin" + table wrapper → strict mode violation)
- **`test.beforeEach` login** — каждый test изолирован (новый context), login через helper. Без `beforeAll`/`afterAll` create/delete test channel — pre-existing канал hardcode (MVP)
- **Job completion timeout 30s** — job на реальном канале с limit=3 занимает ~12s (binary search 13 probes × 1s delay PR#32 + range fetch). 30s — баланс
- **`retries: 1`** — flaky tolerance (network, timing). Если падает 2 раза подряд — реальный баг
- **CI НЕ запускает E2E** — `.github/workflows/ci.yml` не изменён. CI runner не имеет running контейнеров (docker compose в CI сложно). E2E запускаются только локально (`npm run test:e2e`). Follow-up: CI job с docker compose services
- **package.json scripts** — `test:e2e` (все projects), `test:e2e:mobile` (только mobile project)
- **.gitignore** — `test-results/`, `playwright-report/`, `playwright/.cache/` (артефакты: скриншоты, видео, trace, HTML report, browser cache)
- **Knip green без изменений** — `knip.json` `entry` не включает `tests/e2e/` (Playwright tests не part of SvelteKit build). `playwright.config.ts` в корне `web/` — Knip `project` не включает root config files
- **svelte-check НЕ проверяет E2E** — `playwright.config.ts` и `tests/e2e/*.ts` НЕ в `src/`. Biome проверяет все `.ts` (включая tests/) — green